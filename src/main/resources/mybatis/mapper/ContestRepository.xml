<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pers.wjx.ojsb.repository.ContestRepository">
    <insert id="addContest" useGeneratedKeys="true" keyProperty="id">
        insert into contest(author_id, author_username, name, type , description, password, start_time, end_time)
        values (#{authorId}, #{authorUsername}, #{name}, #{type}, #{description}, #{password}, #{startTime}, #{endTime})
    </insert>
    <update id="updateContestDetail">
        update contest
        set name = #{name}, type = #{type}, description = #{description}, password = #{password}
        where id = #{id}
    </update>
    <update id="setContestEndTime">
        update contest
        set end_time = #{endTime}
        where id = #{id}
    </update>
    <update id="setContestTime">
        update contest
        set start_time = #{startTime}, end_time = #{endTime}
        where id = #{id}
    </update>
    <select id="getContestById" resultType="pers.wjx.ojsb.pojo.Contest">
        select *
        from contest
        where id = #{id}
    </select>
</mapper>
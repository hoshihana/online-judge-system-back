<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pers.wjx.ojsb.repository.ParticipationRepository">
    <insert id="addParticipation">
        insert into participation(contest_id, user_id, username, nickname)
        values(#{contestId}, #{userId}, #{username}, #{nickname})
    </insert>
    <delete id="deleteParticipationsByContestId">
        delete from participation
        where contest_id = #{contestId}
    </delete>
    <select id="getParticipation" resultType="pers.wjx.ojsb.pojo.Participation">
        select *
        from participation
        where contest_id = #{contestId} and user_id = #{userId}
    </select>

</mapper>
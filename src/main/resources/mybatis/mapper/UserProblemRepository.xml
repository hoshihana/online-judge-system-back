<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="pers.wjx.ojsb.repository.UserProblemRepository">
    <insert id="increaseTryTimes">
        insert into user_problem(user_id, problem_id, try_times)
        values (#{userId}, #{problemId}, 1)
        on duplicate key update try_times = try_times + 1;
    </insert>
</mapper>